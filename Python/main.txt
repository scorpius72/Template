import sys, math , cmath , time , collections

n,m = map(int,input().split())  -> 1 2  // here n , m different variable 
l = list(map(int,input().split())) -> take the whole array

l.append(value) // this will push back a value

l = sorted(l) // sort the whole l

output in the same line ->
for i in res:
    print(i,end=' ')

print case number:
    print("case #{} :{}".format(i+1,res[i]))